[build-system]
requires = ["maturin>=1.7,<2.0"]
build-backend = "maturin"

[project]
name = "minecraftclone-native"
version = "0.1.0"
description = "Optional Rust terrain kernel for MinecraftClone"
requires-python = ">=3.10"

[tool.maturin]
bindings = "pyo3"
module-name = "minecraftclone_native"
